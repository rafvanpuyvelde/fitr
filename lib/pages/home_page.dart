import 'package:flutter/material.dart';

import 'base_page.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BasePage(child: Text('Test'));
  }
}
